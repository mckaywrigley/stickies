import { GET_STICKIES } from "../actions/types";

const initialState = {
  stickies: []
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
